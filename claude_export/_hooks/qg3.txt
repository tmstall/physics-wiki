labs, sheets, cyclic gluing, partial traces as geometry - **Replica wormholes** — connected saddle points that dominate after the Page time - The **island formula** — minimize generalized entropy over all possible islands; the wormhole pulls the island from traced-over to kept by changing the cyclic ordering - **Entanglement entropy as Shannon entropy** of Schmidt coefficients - The full chain: AdS/CFT → information paradox → Page curve → island What you *don't* have yet, and what Thread D delivers: The island formula tells you *that* information escapes. It doesn't tell you *how* an observer outside the black hole could actually read it out of the Hawking radiation. That question — bulk reconstruction — turns out to have a beautiful answer: **the boundary CFT is a quantum error correcting code, and the bulk fields are the logical operators of that code.** The island and the horizon aren't mysterious once you see them through this lens — they're exactly what error correction predicts. --- ## The question that opens everything Here's the puzzle that makes bulk reconstruction non-trivial. Imagine you're sitting on the boundary of AdS — you have access to the full CFT, which is an ordinary quantum system. Somewhere in the bulk, deep inside, there's a field operator — say, a scalar field at some point well inside the AdS interior. You want to *reconstruct* that bulk operator from boundary data. Can you do it? The answer is yes — but with a catch that turns out to be the whole story. **You can reconstruct any bulk operator from the boundary — but you don't need *all* of the boundary to do it. A sufficiently large subregion of the boundary is enough.** This seems like a technical detail. It isn't. It's the fingerprint of quantum error correction. --- ## Error correction — from the ground up Let me build this from your CS foundation, because you already know the right mental model. Classical error correction: you want to store one bit reliably. You encode it into three bit