We are continuing the MSL v3.14 project in this folder.

I have replaced the previous broken msl\_data.py with a clean, corrected version. From now on, treat the current msl\_data.py in this folder as the single source of truth. Do not regenerate, rewrite, or modify msl\_data.py unless I explicitly ask you to.

Your main task is to generate clean, reliable generator scripts based on the current msl\_data.py. The scripts must correctly read the data and produce well-formatted .docx files.

Key requirements:

- All scripts must support a `-v` / `--verbose` flag that prints the Grok internal version and then generates the file.

- node\_id must be in "section.cluster" format (e.g. "03.07", "06.2", "19.10").

- The "cluster" field must contain only the cluster number as a string (e.g. "1", "2", "10") or be empty ("").

- Do not break node\_id or cluster formatting like we saw in the previous broken version.

- Generate the following scripts:

  - generate\_gold\_list.py

  - generate\_alphabetic.py

  - generate\_audio\_hints.py

  - generate\_highway.py (if still relevant)

  - generate\_all.py (master script that runs all generators)

  - msl\_audit.py (for validating the data)

- The generated documents should include page numbers and headers on pages after the first.

- Avoid page breaks in the middle of a node's output.

- Make the code clean, readable, and defensive.

Once the scripts are generated, I will test them. After that we can refine formatting and features.

Start by confirming you understand the current msl\_data.py structure, then generate the scripts one or two at a time so I can review them.

